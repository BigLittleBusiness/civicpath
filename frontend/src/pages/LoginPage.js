import React, { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../features/AuthContext';

export default function LoginPage() { const [email, setEmail] = useState('demo@civicpath.com.au'); const [password, setPassword] = useState('CivicPathDemo2026!'); const [message, setMessage] = useState(''); const [submitting, setSubmitting] = useState(false); const navigate = useNavigate(); const { signIn } = useAuth(); const submit = async (event) => { event.preventDefault(); setMessage(''); setSubmitting(true); try { await signIn({ email, password }); navigate('/dashboard'); } catch (error) { setMessage(error.response?.data?.error || 'Unable to sign in. Please check your details and try again.'); } finally { setSubmitting(false); } }; return <main className="login-page"><section className="login-aside"><div className="login-brand"><img src="/gate-mark.png" alt=""/><span>CivicPath</span></div><div className="login-note"><p className="card-eyebrow">A clearer route forward</p><h1>See what needs to move next.</h1><p>CivicPath connects Council priorities to project readiness, funding pathways, decisions and delivery.</p><div><span>Priority</span><i/><span>Readiness</span><i/><span>Funding</span><i/><span>Delivery</span></div></div><small>Demonstration environment · Illustrative data only</small></section><section className="login-form-wrap"><form className="login-form" onSubmit={submit}><Link to="/dashboard" className="login-mobile-brand"><img src="/gate-mark.png" alt=""/> CivicPath</Link><p className="card-eyebrow">Welcome to CivicPath</p><h2>Sign in to your workspace</h2><p className="form-lead">Use the pre-filled demonstration credentials to explore the initial product build.</p><label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} /></label>{message && <p className="form-message">{message}</p>}<button className="primary-button login-submit" type="submit" disabled={submitting}>{submitting ? 'Signing in…' : <>Sign in <span>→</span></>}</button><button className="forgot-link" type="button">Forgot password?</button><p className="login-help">Need a CivicPath workspace? <a href="https://www.civicpath.com.au">Explore a Council Proof</a></p></form></section></main>; }
